en:
  activemodel:
    attributes:
      deposit:
        account: "Conta para deposito"
    errors:
      models:
        transaction/deposit:
          attributes:
            amount:
              greater_than: "must be greater than $%{count}"

